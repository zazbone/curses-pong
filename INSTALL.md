Use Makefile only for developpement.

run in main folder:\
`$ python -m pip install -r requirement.txt`\
`$ python -m pip install .`


First command will install dependances (Empty)\
Second install curses-pong program
